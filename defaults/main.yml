---
### defaults file for rhv_provision_rhel7_qcow2
### RHV Data Center Name
datacenter: Default

### RHV Cluster Name
cluster: Default

### RHV Storage Domain
storage_domain: CHANGEME

### VM Template Name from which to build the new VM
vm_template: Blank

### VM Build Parameters
# vm:
#   type: server
#   os: rhel_7x64
#   name: "{{ inventory_hostname }}"
#   hostname: "{{ inventory_hostname }}"
#   cpus: 2
#   sockets: 1
#   memory: 8GiB
#   disk:
#     - name: "{{ inventory_hostname.split('.')[0] }}_{{ inventory_hostname.split('.')[1] }}_disk"
#       size: 60GiB
#   network:
#     - conn_name: eth0
#       ifname: eth0
#       ipv4:
#         method: static
#         address: 172.16.0.21
#         netmask: 255.255.0.0
#         gw: 10.100.0.1
#         dns:
#           - 10.0.0.9
#           - 10.100.0.20
#           - 10.100.0.40
#         dns_search:
#           - hkn.lab

### QCOW2 Image Path for Upload
rhv_image_path: /home/snelson/Documents/DiskImages/VM/KVM/rhel-server-7.7-x86_64-kvm.qcow2
